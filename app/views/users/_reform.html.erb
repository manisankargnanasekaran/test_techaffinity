

<%= form_with(model: report, local: true) do |form| %>
  
  <div class="field">
    <%= form.label :mark %>
    <%= form.text_field :mark, id: :report_mark %>
  </div>

  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
